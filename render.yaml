services:
  - type: web
    name: cp-discord-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    healthCheckPath: /health
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: RENDER_URL
        sync: false
      - key: PREFIX
        value: "!"
      - key: ADMIN_ROLE
        value: "Admin"
      - key: PORT
        value: "10000"
